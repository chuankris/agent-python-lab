# 今日任务：给 `POST /task` 补 1 条成功用例，并整理最小 README 说明

日期：2026-05-17

## 今日目标
在现有最小 `POST /task` 接口基础上，再补 1 条成功路径的 `pytest` 用例，并把 README 里的启动和验证说明整理到“今晚打开就能直接照做”的程度。

## 为什么做这个
昨天如果你已经开始给 `/task` 接口补 Pydantic 模型和失败输入校验，那么今天最自然的下一步不是立刻跳去更复杂的 tool schema，而是先把“成功请求也能被稳定验证”这件事补齐。这样可以把接口输入、输出、测试和说明文档先收拢成一个最小闭环。就算你昨天没做完，今天也依然能成立：先让 `/task` 能稳定返回，再补 1 条成功测试和最少 README 说明，收益依然很高。

## 任务说明
1. 如果仓库里还没有稳定可用的 `POST /task`，先补到“能接收最小合法请求并返回结构化结果”为止，再继续今天任务。
2. 选一个最小成功场景，例如传入一段合法任务文本，接口返回约定好的核心字段；今天只验证这一条正常路径，不要扩展成多场景测试。
3. 新增 1 条最小 `pytest` 用例，重点检查 3 件事：状态码正确、核心返回字段存在、字段值大体符合预期。
4. 如果你昨天已经写了失败用例，今天这条成功用例正好能和它组成“成功 + 失败”最小配对；如果昨天还没写完，也没关系，先把成功用例补上。
5. README 里补最少但够用的说明：怎么启动服务、怎么发送 1 个成功请求、当前 `/task` 最小输入和最小输出是什么。
6. 如果 README 已经有相关内容，就只做整理和校对，避免写成长篇文档；重点是让未来的你 5 分钟内重新跑起来。
7. 今天不要引入真实模型调用、不要实现 tool schema、不要铺开复杂日志；把测试和说明文档这条最小支撑链路补稳就够了。

## 验收标准
1. 本地已有可访问的 `POST /task` 接口，并且存在 1 个最小成功输入示例。
2. 至少新增 1 条围绕成功路径的 `pytest` 用例；如果测试环境已可运行，这条用例应能通过。
3. `/task` 的成功响应至少有清晰、稳定的核心字段，而不是随意拼接的文本。
4. README 或相关说明里，已经写清启动命令、1 个成功请求示例，以及当前接口的最小输入/输出说明。
5. 今天的任务整体仍控制在 20 到 60 分钟内，没有扩展到真实 tool-calling、复杂日志体系或大规模重构。

## 推荐目录/文件
- `app/main.py`
- `app/routes/`
- `app/schemas/`
- `tests/`
- `README.md`

## 推荐 commit message
```text
day020: add success test and task readme notes
```
