# 今日任务：实现最小 `GET /health` 并补一条接口自测

日期：2026-05-12

## 今日目标
在仓库里补出一个最小可访问的 `GET /health` 接口，让 FastAPI 服务第一次真正“跑起来”；如果时间还够，再补 1 条最小 `pytest` 用例，验证接口返回健康状态。

## 为什么做这个
前几天的任务一直在为最小 Agent 服务铺路：README、目录边界、FastAPI 骨架、后续的 Pydantic 和 tool schema 落点。今天这一步的价值，是把“结构”推进到“可运行结果”。只要 `GET /health` 能稳定返回，你后面继续接 `/task`、请求模型、日志和最小 tool-calling flow 时，就有了一个真实的服务入口可以反复验证。即使你昨天还没补完骨架，今天也能先用 10 到 15 分钟补齐最小入口，再继续完成健康检查接口。

## 任务说明
1. 如果仓库里还没有最小 FastAPI 服务入口，先补到“能启动服务”这一层即可，再继续做今天的 `GET /health`。不要顺手展开 `/task`、Pydantic 模型或工具调用逻辑。
2. 在服务里增加一个最小 `GET /health` 路由，返回结构保持简单稳定，例如只表达“服务活着、请求打通了”这一件事，不要提前塞进版本号、依赖检查或复杂字段。
3. 返回体尽量从现在就保持可扩展：字段命名清楚、风格统一，方便后面继续接日志、响应模型或更多状态信息。
4. 如果项目里已经有 `tests/` 目录或你愿意顺手补最小测试入口，就增加 1 条 `pytest` 烟雾测试，只验证 `GET /health` 的状态码和核心返回字段，不把今天扩展成完整测试日。
5. 如果你发现 README 里还没有写清楚如何启动服务或如何跑这条测试，就只补最少几行说明，让“今晚的你”能直接照着运行，不需要回忆命令。
6. 控制工作量在 20 到 60 分钟。今天的重点是打通第一个真实接口，不是把后续异常处理、结构化日志或请求模型一次性做完。

## 验收标准
1. 本地已经有一个可访问的 `GET /health` 接口。
2. 访问该接口时，能返回一个简单、明确、稳定的健康状态结果。
3. 如果补了测试，`pytest` 至少有 1 条围绕 `GET /health` 的用例通过。
4. README 或相关说明里，至少写清楚 1 条启动服务命令；如果补了测试，再写清楚 1 条测试命令。
5. 今天没有把任务膨胀成 `/task`、Pydantic 细化、日志体系或 tool schema 实现，整体仍然是一次短练习。

## 推荐目录/文件
- `app/main.py`
- `app/routes/`
- `tests/`
- `README.md`
- `pyproject.toml`

## 推荐 commit message
```text
day016: add health endpoint and a minimal smoke test
```
