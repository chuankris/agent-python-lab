# 今日任务：整理最小 Agent 服务 README

日期：2026-05-09

## 今日目标
把当前项目的 README 补到“今晚重开仓库也知道怎么跑”的程度，重点写清最小 FastAPI 服务怎么启动、`/task` 或等价入口怎么用、测试怎么跑、格式化和 lint 怎么执行。

## 为什么做这个
这两周你已经围着 FastAPI、Pydantic、日志、pytest、tool schema 和最小 tool-calling flow 一点点往前推。到周末，最值得补的不是再加一个新功能，而是把已经做过的东西整理成一份自己未来看得懂、别人接手也不迷路的说明。对 Agent 工程来说，README 不是装饰，它决定你下次回来是直接继续开发，还是先花 20 分钟回忆项目怎么启动、接口收什么字段、测试命令在哪。即使你前几天有任务漏做，今天这件事也仍然成立，因为 README 可以按“当前真实状态”整理，不要求功能全部完整。

## 任务说明
1. 先通读当前 README，只保留和这个最小 Agent 服务真实状态一致的内容；如果有空话、旧描述或和当前目录不匹配的地方，优先删改，而不是继续堆说明。
2. 至少补上 4 类最小信息：项目目标、如何启动 FastAPI 服务、如何运行 `pytest`、如何执行格式化或 lint。命令不求多，但要让你今晚就能照着跑起来。
3. 如果项目里已经有 `/task` 或等价入口，就补一个最小请求示例，写清输入字段和一个典型返回结构；如果入口还没完整做完，就明确写“当前只支持到哪一步”，不要假装已经闭环。
4. 用 2 到 4 句话交代当前项目里 Pydantic 模型、日志、tool schema 分别扮演什么角色，重点是帮助未来的你快速定位代码，而不是写成长篇设计文档。
5. 如果昨天已经接了 `ruff` 或其他最小格式化入口，就把命令写成固定小节；如果还没接，也可以先把“计划采用什么命令”写清楚，但不要为了补文档反向扩展一整套工具链。
6. 时间够的话，再补一个“推荐目录结构”小节，用 1 行说明 `app/`、`tests/`、`daily/`、`roadmap/` 分别放什么，帮助你后面继续按小步迭代推进。
7. 不要把今天做成大重构，不要顺手补大量新代码。主任务是让说明和当前真实工程状态对齐，并覆盖运行、测试、自检这三件事。

## 验收标准
1. README 已明确写清项目当前目标和最小使用方式，而不是只有仓库口号。
2. 至少包含启动服务、运行测试、执行格式化或 lint 这 3 类命令说明。
3. 如果已有 `/task` 或等价入口，README 里至少有 1 个最小请求示例或状态说明。
4. README 内容和当前仓库真实状态一致，没有明显“文档说有，项目里其实没有”的地方。
5. 整体工作量控制在 20 到 60 分钟，不扩展成新功能开发。

## 推荐目录/文件
- `README.md`
- `app/main.py`
- `app/schemas/`
- `app/tools/`
- `tests/`
- `pyproject.toml`

## 推荐 commit message
```text
day014: document how to run and verify the service
```
